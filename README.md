# Виджет Банк


**Цель проекта:** создать новую фичу для личного кабинета клиента, которая показывает несколько последних успешных банковских операций клиента.


## Основные функции


В проекте реализованы следующие функции:

### Модуль `masks`


* **`get_mask_card_number`** — маскирует номер банковской карты по шаблону «XXXX XX** **** XXXX».
* **`mask_account_card`** — обрабатывает информацию о банковских картах и счетах, возвращая строку с замаскированными данными.
* **`get_date`** — преобразует строку с датой из формата ISO в формат «ДД.ММ.ГГГГ».

### Модуль `processing`

* **`filter_by_state`** — фильтрует список словарей по значению ключа `'state'`.
* **`sort_by_date`** — сортирует список словарей по дате (ключ `'date'`).


### Модуль `widget`

Основной модуль, объединяющий функциональность для отображения виджета с последними успешными операциями клиента.

### Модуль `generators`

Содержит функции для генерации и фильтрации данных по различным критериям:

* **`filter_by_currency`** — фильтрует транзакции по коду или названию валюты.
    * принимает список транзакций и строку с кодом/названием валюты (`"USD"`, `"RUB"`, `"руб."` и т. д.);
    * возвращает только транзакции со статусом `EXECUTED`;
    * ищет совпадения как в поле `code`, так и в поле `name` объекта `currency` внутри `operationAmount`;
    * работает с потоковой обработкой данных (возвращает генератор);
* **`transaction_descriptions`** — извлекает описания транзакций из списка операций.
    * принимает список транзакций;
    * фильтрует только транзакции со статусом `EXECUTED`;
    * извлекает значения из поля `description` каждой транзакции;
    * возвращает генератор строк с описаниями операций;
    * игнорирует транзакции с отсутствующим полем `description`;
* **`card_number_generator`** — генерирует номера банковских карт в заданном диапазоне.
    * принимает два параметра: `start` и `end` (начальное и конечное значения диапазона);
    * генерирует номера карт от `start` до `end` включительно;
    * форматирует номер карты в виде четырёх групп по 4 цифры, разделённых пробелами (`"XXXX XXXX XXXX XXXX"`);
    * проверяет корректность входных параметров (положительные числа, `start <= end`);
    * обрабатывает граничные случаи (минимальные/максимальные значения);
    * возвращает генератор отформатированных номеров карт;

### Модуль decorators
* * Новый модуль, добавляющий функционал логирования выполнения функций через декоратор log.

* * Расположение: src/decorators/__init__.py и src/decorators/log_decorator.py.

### Модуль конвертации валют external_api
* Модуль для конвертации сумм транзакций в рубли с поддержкой валют USD и EUR через внешний API курсов валют.

### Модуль чтения финансовых транзакций - file_reader
* Модуль предоставляет функции для чтения финансовых транзакций из CSV‑ и Excel‑файлов с автоматической очисткой и нормализацией данных.

### Доступные функции

### `read_csv_transactions(file_path)`

* Читает финансовые транзакции из CSV‑файла.

### `read_excel_transactions(file_path)`
* Читает финансовые транзакции из Excel‑файла (.xlsx).

### Модуль src/main.py
* Назначение: главный модуль программы, реализующий пользовательский интерфейс и оркестрацию работы других модулей.

### `Основные функции:`
1. отображение главного меню с выбором действий;
2. обработка ввода пользователя;
3. загрузка данных из файлов разных форматов (JSON, CSV, XLSX);
4. последовательная фильтрация и обработка транзакций:
* по статусу;
* по дате (сортировка);
* по валюте;
* по описанию (поиск);
5. вызов подсчёта транзакций по категориям;
6. вывод результатов в консоль.

### ` Зависимости:`

* count_module;

* file_reader;

* search_module.

### Модуль count_module.py
* Назначение: модуль для подсчёта и анализа транзакций по заданным категориям.

### Модуль search_module.py
* Назначение: модуль для поиска и фильтрации транзакций по ключевым словам в описании.

### Возможности декоратора log
* запись логов в файл или вывод в консоль;

* принудительный вывод в консоль даже при указании файла логов;

* добавление временных меток к сообщениям;

* логирование ошибок с указанием входных параметров;

* безопасная работа с файлами (обработка исключений);

* полная типизация (поддержка mypy).

## Логеры для модуля utils
* logger.info() — информационные сообщения (успехи).

* logger.warning() — предупреждения (файл не найден).

* logger.error() — ошибки (JSON, кодировка).

* logger.critical() — критические ошибки.

### Чек‑лист для проверки:
* [ ] Папка logs создаётся в корне проекта.
* [ ] Файл utils.log перезаписывается при каждом запуске.
* [ ] В логах есть метка времени, название модуля, уровень и сообщение.
* [ ] Все этапы функции read_json_transactions логируются.
* [ ] Ошибки JSON, кодировки и другие исключения записываются в лог.
* [ ] Логгер не дублирует обработчики (проверка if not logger.handlers)

## Логеры для модуля masks
* logger.info() — информационные сообщения:

  * вызов функции;

   * успешный результат.

* logger.debug() — отладочная информация:

  * промежуточные преобразования данных.

* logger.error() — ошибки:

  * нарушение условий (не 16 цифр для карты).

### Чек‑лист для проверки
* [ ] Папка logs создаётся в корне проекта.
* [ ] Файл masking.log перезаписывается при каждом запуске.
* [ ] В логах есть метка времени, название модуля, уровень и сообщение.
* [ ] Все этапы функций get_mask_card_number и get_mask_account логируются.
* [ ] Ошибки (неверная длина карты) записываются в лог с уровнем ERROR.
* [ ] Отладочная информация (преобразование типов) записывается с уровнем DEBUG.
* [ ] Логгер не дублирует обработчики (проверка if not logger.handlers).

## Тестирование


Проект имеет комплексную систему тестирования, построенную на базе `pytest`. Структура тестов организована по модульному принципу для удобства поддержки и расширения.


### Структура тестовых файлов

* **`conftest.py`** — содержит все фикстуры с подробными докстрингами. Фикстуры предоставляют тестовые данные для различных сценариев, включая:
    * валидные и невалидные номера карт;
    * граничные случаи (нули, девятки и т. д.);
    * данные с разными статусами операций (`EXECUTED`, `CANCELED`, `PENDING`);
    * даты в различных форматах ISO;
    * списки с отсутствующими ключами (`state`, `date`).
* **`test_masks.py`** — тесты модуля `masks`. Проверяют корректность маскирования:
    * номеров банковских карт;
    * счетов;
    * обработки различных типов входных данных (строки, числа).
* **`test_processing.py`** — тесты модуля `processing`. Проверяют:
    * фильтрацию операций по статусу (`filter_by_state`);
    * сортировку операций по дате (`sort_by_date`);
    * обработку крайних случаев (пустые списки, некорректные форматы дат, отсутствующие ключи).
* **`test_widget.py`** — тесты модуля `widget`. Проверяют интеграцию всех компонентов для корректного отображения виджета:
    * последовательность вызовов функций;
    * обработку граничных случаев;
    * итоговый формат вывода данных для интерфейса.
* **`test_generators.py`** — тесты модуля `generators`. Проверяют:
    * фильтрацию транзакций по валюте (`filter_by_currency`);
    * извлечение описаний операций (`transaction_descriptions`);
    * генерацию номеров карт (`card_number_generator`);
    * обработку граничных случаев и некорректных входных данных.
* **`test_log_decorators.py`** — тесты модуля `decorators`. Проверяют:
  * Успешное выполнение логов в файл и вывод в консоль
  * Обработка ошибок
  * Многократные вызовы
* **`test_utils.py `** - Тесты для функции read_json_transactions из модуля utils.py, которая читает транзакции из JSON‑файла.
Цель: проверить корректность чтения и парсинга JSON‑файлов с транзакциями, обработку ошибок и крайних случаев.
* **`test_external_api.py `** - Тесты для функции convert_to_rubles из модуля src/external_api.py, которая конвертирует сумму транзакции в рубли.
Цель: проверить логику конвертации валют, обработку ошибок API, граничных случаев и корректность возвращаемых значений.

### Подходы к тестированию

В проекте используются следующие техники тестирования:

* **Фикстуры (`@pytest.fixture`)** — для переиспользования тестовых данных между тестами и модулями.
* **Параметризация (`@pytest.mark.parametrize`)** — для запуска одних и тех же тестов с разными наборами входных данных, что позволяет сократить дублирование кода.
* **Проверка исключений** — тесты гарантируют, что функции корректно обрабатывают некорректные входные данные и выбрасывают ожидаемые исключения.
* **Покрытие граничных случаев** — особое внимание уделено тестированию крайних значений (пустые строки, нули, очень длинные строки и т. п.).

### Запуск тестов

Чтобы запустить все тесты проекта, выполните в корне проекта:
```bash
pytest
